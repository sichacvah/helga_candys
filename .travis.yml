language: ruby
rvm: 
  - "2.1.1"
gemfile: Gemfile
deploy:
  - provider: s3
    access_key_id: <%= ENV["AWS_KEY_ID"] %>
    secret_access_key: <%= ENV["AWS_SECRET"] %>
    bucket: "helgacandys"
    application: helga_candys
